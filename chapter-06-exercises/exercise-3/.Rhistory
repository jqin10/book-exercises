add_three <- function(input) {
output <- sum(input,3)
return(output)
}
add_three <- function(input) {
return(input+3)
}
ten <- add_three(7)
print(ten)
imperial_to_metric <- function(feets,inches){
meters <- feet(0.3048)+inches(0.0254)
return(meters)
}
height_in_meters <- imperial_to_metric(5,5)
add_three <- function(input) {
return(input+3)
}
ten <- add_three(7)
imperial_to_metric <- function(feets,inches){
meters <- feets(0.3048)+inches(0.0254)
return(meters)
}
# Create a variable `height_in_meters` by passing your height in imperial to the
# `imperial_to_metric` function
height_in_meters <- imperial_to_metric(5,5)
# Create a variable `height_in_meters` by passing your height in imperial to the
# `imperial_to_metric` function
height_in_meters <- imperial_to_metric(5 , 5)
# Create a variable `height_in_meters` by passing your height in imperial to the
# `imperial_to_metric` function
height_in_meters <- imperial_to_metric(5 feets, 5 inches)
imperial_to_metric <- function(feets+inches){
meters <- feets(0.3048)+inches(0.0254)
return(meters)
}
imperial_to_metric <- function(feets,inches){
meters <- feets(0.3048)+inches(0.0254)
return(meters)
}
ten <- add_three(7)
add_three <- function(input) {
return(input+3)
}
ten <- add_three(7)
# Create a variable `height_in_meters` by passing your height in imperial to the
# `imperial_to_metric` function
height_in_meters <- imperial_to_metric(5)
# Create a vector `names` that contains your name and the names of 2 people
# next to you. Print the vector.
names <- c("Jennifer", "Sereen", "Lily")
# Use the colon operator : to create a vector `n` of numbers from 10:49
n <- 10:49
add_three <- function(input) {
return(input+3)
}
ten <- add_three(7)
imperial_to_metric <- function(feets,inches){
meters <- feets*0.3048+inches*0.0254
return(meters)
}
# Create a variable `height_in_meters` by passing your height in imperial to the
# `imperial_to_metric` function
height_in_meters <- imperial_to_metric(5,5)
?length
# Use the `length()` function to get the number of elements in `n`
length(n)
# Use the colon operator : to create a vector `n` of numbers from 10:49
n <- 10:49
# Use the `length()` function to get the number of elements in `n`
length(n)
# Use the `seq()` function to produce a range of numbers from -5 to 10 in `0.1`
# increments. Store it in a variable `x_range`
x_range <- seq (-5, 10, 0.1)
# Create a vector `sin_wave` by calling the `sin()` function on each element
# in `x_range`.
sin_wave <- sin(x_range)
cos_wave <- cos(x_range)
# Create a vector `wave` by multiplying `sin_wave` and `cos_wave` together, then
# adding `sin_wave` to the product
wave <- sin_wave*cos_wave + sin_wave
# Use the `plot()` function to plot your `wave`!
plot(wave)
print(n+1)
# Create a vector `m` that contains the numbers 10 to 1 (in that order).
# Hint: use the `seq()` function
m <- c(seq(10,1))
# Subtract `m` FROM `n`. Note the recycling!
m-n
# Subtract `m` FROM `n`. Note the recycling!
m-n
# Subtract `m` FROM `n`. Note the recycling!
n-m
